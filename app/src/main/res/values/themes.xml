<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme using Material You -->
    <style name="Theme.ByeDPI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here if needed -->
        <item name="colorPrimaryContainer">@color/md_theme_primaryContainer</item>
        <item name="colorOnPrimaryContainer">@color/md_theme_onPrimaryContainer</item>

        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Widget.ByeDPI.Button" parent="Widget.Material3.Button">
        <item name="android:textAppearance">@style/TextAppearance.ByeDPI.ButtonText</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingEnd">24dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
    </style>

    <style name="TextAppearance.ByeDPI.ButtonText" parent="TextAppearance.Material3.LabelLarge">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="Widget.ByeDPI.Card" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">2dp</item>
    </style>

    <style name="PreferenceThemeOverlay.ByeDPI" parent="PreferenceThemeOverlay.Material3">
        <item name="android:layout">@layout/preference_material</item>
    </style>
</resources>